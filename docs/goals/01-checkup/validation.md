# Final cross-system validation checklist

Planning artifact, not a record of successful implementation. No box is passed yet. Use the exact command blocks and story-specific commands in [stories.md](stories.md#command-conventions-and-shared-checks); S/B/D/M/V/RM/RV below reference those blocks. Execute only in a later authorized session on the named hosts.

Each result records UTC time, host, approved implementation commit, command/action, exit status or observable result, and evidence reference. Paste concise sanitized outcomes into the owning story's “Evidence to record” field; consolidate longer sanitized output in EVIDENCE.md. Never paste secret values, hashes used as credentials, message contents, private keys or authentication traces. Record provider IDs and file metadata only where needed for recovery.

A checkbox passes only with its stated evidence. “Unknown” is permitted for historical provenance/installer lineage and other explicitly retained historical facts; it is not permitted for activation prerequisites. Conditional checks may be marked not applicable only with the condition and evidence recorded. Planning does not satisfy these gates.

## Source, release and build gates — before either activation

- [ ] **R-REPO-04 / US-01:** Existing linkApps profile edit and AGENTS.md are preserved and their inclusion has an explicit implementation decision. Any other pre-existing user changes are accounted for without being discarded.
- [ ] **R-REPO-04 / US-01, US-03:** Record one approved full clean implementation commit, its lock hash and all locked input revisions. Both native build checkouts report that commit with `git rev-parse HEAD` and pass `test -z "$(git status --porcelain)"`. Evidence-only Markdown recorded later does not redefine the implementation commit; identify both revisions separately if needed.
- [ ] **R-REPO-01/02 / US-02:** Supported stable input compatibility is verified on implementation day with dated official release/migration sources. Shared nixpkgs is retained unless a demonstrated need justifies separation; every package-specific unstable exception is named and justified.
- [ ] **R-REPO-02, R-VPS-05 / US-02, US-03:** Nix, Home Manager, mailserver and Dovecot migrations are reviewed from both actual deployed baselines. A direct path is recorded, or each documented required intermediate has its own build/recovery/acceptance evidence. State-format rollback safety after writes is explicitly classified.
- [ ] **R-REPO-02 / US-02:** `rg -n 'stateVersion' machines profiles modules hardware` matches the recorded baseline: Darwin system 5, NixOS system 24.05, both home states 24.05, mailserver 3. Any change has a separate explicit migration decision; none is incidental.
- [ ] **R-REPO-05 / US-02:** Candidate source contains no embedded credential values before flake evaluation/materialization. Runtime references do not import plaintext into Git, logs or the store. Secret ownership, permissions, provisioning order, exact recovery procedure and rotation decisions are documented without values. Historical exposure is not described as erased.
- [ ] **R-REPO-03 / US-03:** S passes: both explicit drvPath evaluations, `nix flake check --no-build --all-systems --no-update-lock-file --no-write-lock-file`, and `git diff --check`. Record full sanitized errors/warnings and resolution/disposition; the historical source-lock permission failure is not treated as a configuration verdict.
- [ ] **R-REPO-03 / US-03:** B passes natively on Mac and x86_64-linux VPS **locally**, before either activation, at the same approved commit/lock. If a capacity decision changed build arrangements, document exact authorized builder/target commands and renew this evidence. CI alone is insufficient.
- [ ] **R-REPO-03/04 / US-03:** Both canonical output closures are recorded; lock hash is unchanged across evaluation/build; builds have not changed either active or selected system profile.
- [ ] **R-REPO-01/02/03 / US-02, US-03:** D is reviewed against each actual deployed baseline. Every removed service/package, major version jump and size delta has an explicit disposition. Live service inventory and target enablement are compared separately; closure differences alone do not prove services remain enabled.
- [ ] **R-REPO-06 / US-02:** PR CI at the candidate revision explicitly evaluates both host outputs and checks the locked flake, succeeds without deployment credentials, and records native-runner limitations. This is mandatory under the user's SC-11 clarification.
- [ ] **R-REPO-07/08 / US-02:** README paths and host-specific maintenance/recovery procedures match the repository; historical unknown source attribution stays unknown. Spotify/comment/Dovecot workaround review has a retain/remove decision based on observed use and compatibility. R-REPO-08 retains its should priority; record any accepted deferral explicitly.

## Mac readiness and acceptance — first deployment window

- [ ] **R-MAC-01 / US-01:** Actual generation/closure, runtime Nix owner, daemon state, build-user names/UIDs, root/user channels, integrated/standalone Home Manager ownership and package inventory are recorded. Partial inventory is not represented as complete; unknown installer history has not triggered replacement or UID changes.
- [ ] **R-MAC-03/04 / US-01:** User has approved a named application/workflow checklist with resolved paths and ownership; duplicates affecting app resolution are identified. Manual Homebrew formula/cask baseline is complete, and no-cleanup/no-auto-update target policy is verified.
- [ ] **R-MAC-02 / US-04:** Generation 34 or the actual newer pre-change generation is retained and rooted. `nix store verify --recursive --no-trust "$MAC_OLD"` succeeds; old rebuild entry and RM generation-selection procedure are verified for the installed/target tools.
- [ ] **R-MAC-02 / US-04:** Verified user-data/mutable-settings recovery covers each named workflow. Tested independent local console/recovery-shell access and exact separate data/secret recovery procedures are available before high-risk activation.
- [ ] **R-MAC-05 / US-04:** A separate two-hour Mac window and critical-workflow failure stop/recovery trigger are recorded. No VPS activation window is scheduled before passing Mac acceptance.
- [ ] **R-MAC-03 / US-04:** Target-native build, clean commit/lock, D and target-version `darwin-rebuild check` pass before switch. Record any point after which generation rollback cannot reverse mutable state.
- [ ] **R-REPO-04, R-MAC-03 / US-04:** `darwin-version --configuration-revision` equals the approved implementation commit; active and selected paths equal the recorded built Mac target.
- [ ] **R-MAC-03/04 / US-04:** M passes: fresh login, daemon/ping, Zsh/Git, each named app's discovery/launch/workflow and linkApps resolution, unchanged manual Homebrew inventory, runtime secret metadata/login where affected, and free disk space above the recorded reserve.
- [ ] **R-MAC-02/05 / US-04:** Pass/fail and timestamps are recorded. If recovery occurred, RM plus separate mutable-data/secret recovery was verified by M and critical workflows. “Recovered to baseline” is not target acceptance; VPS scheduling remains blocked until the Mac target is accepted.

## VPS readiness — preparation outside its outage window

- [ ] **R-VPS-01 / US-01:** Actual paths, owners/modes, filesystems and backup coverage are recorded for mailbox state, delivery queues, Sieve, credentials, DKIM, ACME and persistent Rspamd/Redis, with evidence of absence when a category does not exist. Only metadata is retained.
- [ ] **R-VPS-02 / US-01, US-05:** A completed full-system snapshot or equivalent off-host backup has an identifier, timestamp, verified coverage/consistency and retention record. All coverage gaps are resolved; “snapshot requested” is not completion.
- [ ] **R-VPS-03 / US-01, US-05:** Independent Vultr console login is tested before the window; actual baseline generation/closure is retained and verified. Its boot-menu entry is selected successfully on the isolated restore; observed production boot/hardware configuration has been compared with the repo before any alteration.
- [ ] **R-VPS-02/10 / US-05:** Isolated restoration succeeds without competing public mail ingress or egress, including IPv6. Restored state coverage and controlled mail access are verified. Exact selected provider restore, runtime-secret recovery and post-snapshot reconciliation procedures are recorded and tested; no placeholder procedure passes.
- [ ] **R-VPS-09/10 / US-05:** Measured total recovery includes preserving newer accepted messages/queues, restoration, reconciliation, service/mail/access verification and secret recovery. A positive safety margin is named. Synthetic accepted-message IDs have no unexplained losses; duplicates have dispositions.
- [ ] **R-VPS-04 / US-03:** Numeric capacity budget covers incremental closure, peak build/scratch space, retained rollback closures and mail growth headroom; byte/inode availability meets the budget. If insufficient or unbounded, deployment is stopped for an agreed capacity plan. GC is not an assumed fix.
- [ ] **R-VPS-05 / US-03:** x86_64-linux build completed before the outage, and actual closure/space observations are reconciled with the budget. Option/data-format migrations and old-software read compatibility are documented.
- [ ] **R-VPS-06/07 / US-01, US-06:** Owners/purposes of 443 and 12340 and host/provider IPv4/IPv6 rules are known. Required SSH/mail/ACME access matches the intended service map, with no extra port opened merely because it listens.
- [ ] **R-VPS-06 / US-06:** Controlled pre-change reception, authenticated submission on the actual used port, IMAPS login/message access, outbound external receipt and every configured alias test pass. TLS chain/name/expiry and intended MX/A/AAAA/SPF/DKIM/DMARC plus delivered authentication results pass. Protect credentials and record only synthetic IDs and result metadata.
- [ ] **R-VPS-07 / US-06:** SSH succeeds from a second authorized session over IPv4 and IPv6 while the original stays open. V records zero failed units, required units active, intended listeners and disk reserve.
- [ ] **R-VPS-09 / US-06:** Separate two-hour VPS window is scheduled after passing Mac acceptance. Latest recovery start is explicitly recorded as `outage start + 60 minutes − measured total recovery − safety margin`. No activation begins if that leaves no feasible recovery interval. Backup/build preparation is already complete.

## VPS activation and recovery checks

- [ ] **R-VPS-03/05/09 / US-06:** Same clean commit/lock/native output and fresh backup/access/capacity preconditions are reconfirmed. Any critical source or baseline change invalidates corresponding evidence. Record the first potentially incompatible persistent write; `test` can already cause it.
- [ ] **R-VPS-06/07/08 / US-06:** `nixos-rebuild test` runs before switch/boot selection. V and the full US-06 suite pass while active closure is target and selected persistent closure remains baseline. Both second-session SSH checks pass before closing the first session.
- [ ] **R-VPS-08 / US-06:** After switch, active and selected closures equal the built target and `nixos-version --configuration-revision` equals the approved commit. Repeat V and all mail/TLS/DNS/authentication tests, including secret metadata and disk reserve.
- [ ] **R-VPS-08 / US-06:** If a reboot was planned, it completes with console available; booted/active/selected paths match target, and V plus US-06 pass again. Otherwise record the explicit “no reboot planned” decision; do not claim the new kernel/boot path was tested by switch alone.
- [ ] **R-VPS-09 / US-06:** Timestamped mail interruption, including any recovery, totals at most 60 minutes. Recovery starts by the predetermined deadline if needed. A two-hour work window does not extend the outage limit; a violation is recorded as failure.
- [ ] **R-VPS-10 / US-06:** Every message accepted after the backup boundary is preserved or reconciled with zero unexplained missing IDs; queue/mailbox duplicates are accounted for. Do not overwrite newer state with an older snapshot without the reviewed/tested reconciliation procedure.
- [ ] **R-VPS-03/05/10 / US-06:** If recovery was used, RV was used only with compatible data; otherwise the tested restore/reconciliation procedure was followed. Old closure/generation identity, zero failed units, full mail functionality, both SSH paths and disk reserve prove recovery. A recovered baseline is not target acceptance.

## Legacy entry points, maintenance and generations retained

- [ ] **R-VPS-11 / US-07:** The authoritative flake source is documented. Legacy file retirement has a verified backup and tested out-of-band console precondition because archived files may contain credential/recovery state. Root nixos-23.05 channel, login-user channels and February 2024 files each have proven consumer/nonuse findings. Retire only proven-unused legacy entry points, preserving protected recovery copies/metadata; record an explicit retained-use or unresolved decision otherwise. The PRD does not require deleting every channel, nor Mac channels. This requirement is mandatory under the clarification, with conditional retirement as written in the PRD.
- [ ] **R-VPS-12 / US-07:** A named recurring backup owner, schedule, retention period, restore-check cadence and next maintenance/recovery-check date are recorded. These values are chosen during implementation; the PRD supplies no numeric ongoing retention/cadence.
- [ ] **R-MAC-02, R-VPS-03 / US-04, US-01, US-07:** Generation lists show both actual pre-change and accepted target generations on each host; any required intermediate recovery generation remains if the migration plan needs it. Retain the complete referenced closures and recovery entry points. Do not substitute generation 34/21 if later baseline discovery found newer active generations.
- [ ] **PRD §8, R-MAC-02, R-VPS-03/04 / US-07:** Record existing GC schedules and generation-expiry policy through targeted safe configuration/scheduler inspection. No GC or automatic generation deletion occurs as an assumed prerequisite. Ensure scheduled cleanup cannot remove the retained baseline during the acceptance/recovery period; if it can, resolve the retention conflict before activation. There is no PRD-approved universal “keep N generations” or age threshold: retain baseline plus accepted target and any required intermediates until an explicit later retirement decision, and document the final policy.
- [ ] **R-VPS-02/10/12 / US-07:** Verified backup and post-snapshot mail preservation remain available under recorded retention; any future expiry/deletion decision accounts for mail reconciliation and measured recovery, independently of Nix generation retention.

## Final same-commit and evidence closure

- [ ] **R-REPO-04, R-VPS-08 / US-07:** Compare recorded output of `darwin-version --configuration-revision` on Mac with `nixos-version --configuration-revision` on VPS. Both equal the same approved full implementation commit; archive corresponding lock revisions and active/selected closure paths. Input matches without configuration revision proof do not pass.
- [ ] **SC-01–SC-11 / US-07:** All twenty PRD must requirements plus the four user-promoted SC-11 requirements have passing mapped stories. R-REPO-08's outcome/deferral is recorded. Every acceptance statement has binary evidence; incomplete parameters, missing native builds, untested backups or unknown current access cannot be marked complete.
- [ ] **R-REPO-07 / US-07:** Runbook links and exact tested recovery commands are retained with consolidated sanitized evidence; both original baseline and final target remain identifiable. No standalone per-command logs or one-off collectors are added to the repo.
- [ ] **Planning integrity:** This planning session's changes are restricted to `docs/goals/01-checkup/`. Initial status already included modified `profiles/home-darwin.nix` and untracked `AGENTS.md`; they must remain untouched. Assess `git status --short --untracked-files=all` relative to that baseline, not by deleting or committing unrelated user work. No `.nix` or lock change, build, activation, SSH, commit, push or secret disclosure is permitted during this planning session.

## Gaps that must be filled during implementation

The priority conflict was resolved by the user: R-REPO-06/07 and R-VPS-11/12 are mandatory. The prior PRD exclusion of stories referred to the earlier session; this request explicitly authorizes these planning documents. The globally clean-status condition conflicts with pre-existing outside-directory changes; only this session's change boundary can be verified while preserving them.

Remaining PRD discovery gates are exact supported input revisions/intermediate migrations, normal-store evaluation results, current deployed provenance and ownership, the user-approved Mac app/data-recovery checklist, actual mail-state paths, completed backup/isolation/restore/reconciliation evidence, runtime-secret mechanism/rotation/recovery commands, listener/provider-firewall purpose, capacity reserves, recurring retention/cadence and measured recovery timing. No assumed answer or unimplemented story is represented as a pass.
